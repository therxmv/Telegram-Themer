# Theme verification summary

Generated by `theme-wizard/verification/generate_themes.py` + `check_contrast.py` against `theme-wizard/templates/`, across 9 representative accent/mode scenarios per platform/style (see `generate_themes.py`'s `SCENARIOS`). Raw per-(platform,style) data is in the sibling `.json` files in this folder.

## 1. Template bugs (accent-independent - broken in every generated theme)

### android_default
Key's role isn't a real role name (typo, or a literal value the resolver doesn't support). The real app silently falls back to solid black for these, regardless of accent:

- `chat_BlurAlpha` (light) -> role `"#BE000000"` (not a valid role)
- `chat_BlurAlphaSlow` (light) -> role `"#99000000"` (not a valid role)
- `chat_BlurAlpha` (dark) -> role `"#BE000000"` (not a valid role)
- `chat_BlurAlphaSlow` (dark) -> role `"#99000000"` (not a valid role)

### android_soza
Key's role isn't a real role name (typo, or a literal value the resolver doesn't support). The real app silently falls back to solid black for these, regardless of accent:

- `chat_BlurAlpha` (light) -> role `"#BE000000"` (not a valid role)
- `chat_BlurAlphaSlow` (light) -> role `"#99000000"` (not a valid role)
- `chat_BlurAlpha` (dark) -> role `"#AA000000"` (not a valid role)
- `chat_BlurAlphaSlow` (dark) -> role `"#80000000"` (not a valid role)

## 2. Android: alpha lost on export (translucent roles ship opaque)

`AndroidThemeFileAdapter`'s writer (`Int.colorToHex()`, see `app/src/main/java/com/therxmv/telegramthemer/data/extensions/ColorExtensions.kt`) strips alpha from every value with no `tr_*` special-case (the iOS adapter does have one). Every key below is templated to a translucent role but will render as a solid, fully opaque color in the real generated `.attheme` file - not the faded overlay the template intends.

### default (95 affected keys)
- `tr_accent_5`: 19 keys, e.g. `chat_inFileBackground`
- `tr_accent_7`: 17 keys, e.g. `chat_inFileBackground`
- `tr_gray_5`: 4 keys, e.g. `actionBarDefaultSearchPlaceholder`
- `transparent_0`: 24 keys, e.g. `actionBarActionModeDefaultTop`

### soza (129 affected keys)
- `tr_accent_5`: 10 keys, e.g. `chat_inTextSelectionHighlight`
- `tr_accent_7`: 1 keys, e.g. `chat_outLinkSelectBackground`
- `tr_gray_3`: 29 keys, e.g. `actionBarActionModeDefaultSelector`
- `tr_gray_5`: 27 keys, e.g. `actionBarActionModeDefaultSelector`
- `transparent_0`: 23 keys, e.g. `actionBarActionModeDefaultTop`

## 3. Systemic contrast failures (fail across ALL representative scenarios)
Likely a palette/ramp issue (or the role pairing itself is wrong) - not fixable by nudging one template key, since every accent hits it.

None found.

## 4. Isolated contrast failures (fail for SOME accents only)
Likely fixable at the template level - reassign that one key to a different existing role (a step lighter/darker on the same ramp).

### android_default
- **unread_counter** (`chats_unreadCounter` bg / `chats_unreadCounterText` fg, needs >=4.5:1): fails 7/9 (dark-blue-amoled, dark-blue, dark-deep-purple, dark-red, light-blue, light-deep-purple, light-red), worst 1.09:1 (#2c0c54 vs #181818, `dark-deep-purple`)
- **out_file_info** (`chat_outBubble` bg / `chat_outFileInfoText` fg, needs >=4.5:1): fails 5/9 (dark-blue-amoled, dark-blue, dark-pale-yellow, dark-red, light-pale-yellow), worst 1.67:1 (#007aff vs #a7a7a7, `dark-blue-amoled`)
- **out_bubble_text** (`chat_outBubble` bg / `chat_messageTextOut` fg, needs >=4.5:1): fails 4/9 (dark-blue, dark-deep-purple, dark-red, light-pale-yellow), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **out_reply_sender** (`chat_outBubble` bg / `chat_outReplyNameText` fg, needs >=4.5:1): fails 4/9 (dark-blue, dark-deep-purple, dark-red, light-pale-yellow), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **out_reply_text** (`chat_outBubble` bg / `chat_outReplyMessageText` fg, needs >=4.5:1): fails 4/9 (dark-blue, dark-deep-purple, dark-red, light-pale-yellow), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **out_file_name** (`chat_outBubble` bg / `chat_outFileNameText` fg, needs >=4.5:1): fails 4/9 (dark-blue, dark-deep-purple, dark-red, light-pale-yellow), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **out_audio_duration** (`chat_outBubble` bg / `chat_outAudioDurationText` fg, needs >=4.5:1): fails 4/9 (dark-blue, dark-deep-purple, dark-red, light-pale-yellow), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **avatar_icon_on_bg** (`avatar_backgroundBlue` bg / `avatar_actionBarIconBlue` fg, needs >=3.0:1): fails 2/9 (dark-deep-purple, light-deep-purple), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)

### android_soza
- **unread_counter** (`chats_unreadCounter` bg / `chats_unreadCounterText` fg, needs >=4.5:1): fails 7/9 (dark-blue-amoled, dark-blue, dark-pale-yellow, dark-red, light-blue, light-deep-purple, light-red), worst 1.41:1 (#ffd54f vs #ffffff, `dark-pale-yellow`)

### ios_default
- **out_bubble_secondary** (`chat.message.outgoing.bubble.withoutWp.bg` bg / `chat.message.outgoing.secondaryText` fg, needs >=4.5:1): fails 5/9 (dark-blue-amoled, dark-blue, dark-pale-yellow, dark-red, light-pale-yellow), worst 1.67:1 (#007aff vs #a7a7a7, `dark-blue-amoled`)
- **out_bubble_primary** (`chat.message.outgoing.bubble.withoutWp.bg` bg / `chat.message.outgoing.primaryText` fg, needs >=4.5:1): fails 4/9 (dark-blue, dark-deep-purple, dark-red, light-pale-yellow), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **in_reaction_active** (`chat.message.incoming.bubble.withoutWp.reactionActiveBg` bg / `chat.message.incoming.bubble.withoutWp.reactionActiveFg` fg, needs >=3.0:1): fails 2/9 (dark-deep-purple, light-deep-purple), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **inputpanel_send_button** (`chat.inputPanel.actionControlBg` bg / `chat.inputPanel.actionControlFg` fg, needs >=3.0:1): fails 2/9 (dark-deep-purple, light-deep-purple), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)

### ios_soza
- **in_reaction_active** (`chat.message.incoming.bubble.withoutWp.reactionActiveBg` bg / `chat.message.incoming.bubble.withoutWp.reactionActiveFg` fg, needs >=3.0:1): fails 2/9 (dark-deep-purple, light-deep-purple), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)
- **inputpanel_send_button** (`chat.inputPanel.actionControlBg` bg / `chat.inputPanel.actionControlFg` fg, needs >=3.0:1): fails 2/9 (dark-deep-purple, light-deep-purple), worst 1.5:1 (#4a148c vs #181818, `dark-deep-purple`)

## 5. Indistinguishable states (two roles that must differ but don't)

None found.
